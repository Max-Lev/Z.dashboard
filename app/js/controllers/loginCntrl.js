define(['app', 'ngStorage'], function (app, ngStorage) {    app.controller('loginCntrl', ['$scope', '$localStorage',        function ($scope, $localStorage) {            console.log('loginCntrl init');            console.log($localStorage);            $scope.productName = $localStorage.clientName;            $scope.addProduct = function (product) {                console.log(product);                $scope.productName = product;                $localStorage.$default({clientName: $scope.productName});                $localStorage.clientName = $scope.productName;            };        }]);});